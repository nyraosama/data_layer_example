name: data_layer_example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  cupertino_icons: ^1.0.2
  json_annotation: ^4.4.0
  logger: ^1.1.0
  path: ^1.8.0
  path_provider: ^2.0.9
  provider: ^6.0.3
  sqflite: ^2.0.2
  build_runner: ^2.1.0
  json_serializable: ^6.1.5


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true

